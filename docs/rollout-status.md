# Rollout status

This document is updated during commissioning. Automatic callers are deployed only after the central reusable workflow is merged to `main`, because cross-repository reusable workflow calls resolve the referenced default-branch workflow.
